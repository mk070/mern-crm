// const schedulePost = (post) => {
//     const { scheduledTime } = post;

//     // Calculate delay in milliseconds until scheduledTime
//     const delay = new Date(scheduledTime).getTime() - Date.now();

//     // Schedule the post using setTimeout (example)
//     setTimeout(() => {
//         // Example: Implement Instagram posting logic here
//         console.log(`Scheduled post ${post._id} published.`);
//     }, delay);
// };

// module.exports = {
//     schedulePost
// };
